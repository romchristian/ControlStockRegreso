<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!--CheckBoxPreference
        android:key="example_checkbox"
        android:title="@string/pref_title_social_recommendations"
        android:summary="@string/pref_description_social_recommendations"
        android:defaultValue="true" /-->

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <!--EditTextPreference
        android:key="telefonoid"
        android:title="Telefono ID"
        android:defaultValue="NULL"
        android:selectAllOnFocus="true"
        android:inputType="none"
        android:capitalize="words"
        android:singleLine="true"
        android:maxLines="1" /-->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="url_servidor"
        android:title="Servicio al que apunta"
        android:defaultValue="0"
        android:entries="@array/pref_servidor_titles"
        android:entryValues="@array/pref_servidor_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />
    <Preference
        android:title="Actualizar BD"
        android:key="actualizarBD"/>
    <Preference
         android:title="Telefono ID"
         android:key="telefonoid"/>
 </PreferenceScreen>
